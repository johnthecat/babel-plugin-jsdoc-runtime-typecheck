//@typecheck

/**
 * @param {Number} a
 * @param {Number} b
 * @param {Number} c
 * @returns {Number}
 */
function test(a, b, c) {
    return a + b + c;
}

/**
 * @param {String} a
 * @returns {Number}
 */
function parse(a) {
    return parseInt(a, 10);
}
